package com.example.restservice;

// Record is a data class created for convenience.
// The only things required when creating it are attributes and their data types.
public record Greeting(long id, String content) { }